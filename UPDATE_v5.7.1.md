# Twitter 插件 v5.7.1 更新日志

**发布日期**: 2026-07-31

## 🎉 重大更新 - 完整私信系统

### 1. 发送逻辑优化 ⭐

**用户需求**：
"回车键不能发送！是只有按发送键发送的才可以看成是一次请求回复！"

**实现**：
- **回车键** → 只发送消息，不触发 AI 回复
- **发送按钮** → 发送消息 + 触发 AI 回复

**使用场景**：
```
情况1：快速聊天
用户：你好 [回车]
用户：今天天气不错 [回车]
用户：有空吗 [回车]
（三条消息快速发送，不触发 AI）

情况2：需要回复
用户：你觉得呢？[点击发送按钮]
（发送消息 + 触发 AI/NPC 回复）
```

---

### 2. 美化分享推文 🎨

**用户需求**：
"分享帖子全是文字太丑了，你要像推特一样搞一个html包裹的分享贴功能"

**修复前**：
```
【分享推文】

@username: 这是推文内容

—— 来自 Twitter
```

**修复后**：
漂亮的推文卡片，包含：
- 作者头像和用户名
- 推文完整内容
- 点赞数和转发数（带图标）
- "来自 Twitter" 标识
- 根据发送者调整配色：
  - 自己发送：蓝色背景 (#1d9bf0)
  - 对方发送：浅灰背景 (#eff3f4)

**技术实现**：
```javascript
// 推文卡片数据结构
{
  type: 'tweet_card',
  tweetId: tweet.id,
  author: { name, username, avatar },
  content: '推文内容',
  likes: 123,
  retweets: 45
}
```

---

### 3. 删除消息功能 🗑️

**用户需求**：
"聊天界面user可以自己选择删消息"

**功能**：
- **长按消息**（移动端）→ 显示删除确认
- **右键点击消息**（桌面端）→ 显示删除确认
- 只能删除自己发送的消息
- 删除后自动保存并刷新界面

**删除流程**：
```
1. 长按/右键点击自己的消息
2. 弹出确认对话框
   - "删除消息"
   - "此操作无法撤销"
   - [取消] [删除]
3. 点击删除
4. 消息被移除
5. 显示"消息已删除"提示
```

---

## 🔧 技术改进

### 消息渲染系统

创建统一的消息渲染函数：
```javascript
renderChatMessage(message, isOwn, messageIndex)
```

**支持两种消息类型**：
1. 普通文本消息
2. 推文卡片消息（JSON 格式）

**优势**：
- 代码复用
- 易于扩展新消息类型
- 统一样式管理

### 私信系统架构

**两个独立的聊天系统**：

1. **Twitter 用户私信**（`openChat`）
   - 用户之间的对话
   - 不调用 AI API
   - 直接保存到 `twitterData.conversations`

2. **Roche Char 对话**（`openChatWithConv`）
   - 和 AI 角色的对话
   - 调用 `roche.ai.chat` API
   - 保存到 `twitterData.chatHistory`

---

## 📊 完整功能列表

### 私信系统
- ✅ 显示 Twitter 用户列表
- ✅ 新建消息（搜索用户）
- ✅ 发送文本消息
- ✅ 分享推文卡片
- ✅ 删除自己的消息
- ✅ 回车快速发送
- ✅ 按钮触发 AI 回复
- ✅ 消息持久化保存
- ✅ 未读标记
- ✅ 消息筛选（全部/未读）

### 推文功能
- ✅ 发推文
- ✅ 点赞/转发/评论
- ✅ 书签
- ✅ 分享到私信（卡片样式）
- ✅ 关注用户
- ✅ 翻译推文

### NPC 系统
- ✅ 自动发推文
- ✅ 智能回复
- ✅ 兴趣度计算

---

## 📦 部署信息

**版本号**：v5.7.1  
**类型**：功能增强

**安装链接**：
```
https://raw.githubusercontent.com/zxinyi404-maker/-roche-twitter/main/plugin.js?v=20260801100000
```

**代码变更**：
- `renderChatMessage()` - 统一消息渲染，支持推文卡片
- `shareTweetToConversation()` - 生成推文卡片数据
- `showDeleteMessageDialog()` - 删除消息确认对话框
- `openChat()` - 完善发送逻辑 + 删除事件绑定

---

## 🎊 总结

v5.7.1 是一个重要的完善版本！

**核心改进**：
1. ✅ 发送逻辑更合理（回车/按钮分离）
2. ✅ 分享推文更美观（HTML 卡片）
3. ✅ 可以删除自己的消息

**用户体验**：
- 聊天更流畅
- 分享更美观
- 操作更灵活

现在 Twitter 插件的私信系统已经非常完善了！🎉

---

*发布日期：2026-07-31*  
*开发者：zxinyi404*  
*GitHub：https://github.com/zxinyi404-maker/-roche-twitter*
