# Twitter 插件 v5.6.3 更新日志

**发布日期**: 2026-07-31

## 🔄 联系人列表同步优化

### 问题
用户反馈："私信页面那个蓝色按键可以查看联系人那个，这个记录要和 Twitter 聊天内容同步哈"

### 修复内容

**修复前**：
- 新建消息对话框显示的是"记忆摘要"
- 没有显示最后一条消息
- 没有时间信息
- 不知道哪些对话有新消息

**修复后**：
- ✅ 显示最后一条消息内容（最多50字）
- ✅ 显示消息时间（如"3分钟前"）
- ✅ 按最后消息时间排序（最新的在最前面）
- ✅ 和私信列表完全同步

---

## 🎨 界面优化

### 新建消息对话框

**标题变更**：
- 修复前：`新建私信`
- 修复后：`选择联系人` ✅

**每个联系人显示**：
```
头像   [联系人名字]          [3分钟前]
       [最后一条消息内容...]
```

**排序逻辑**：
- 按最后消息时间排序
- 最近聊天的在最上面
- 方便快速找到正在进行的对话

---

## 🔧 技术实现

### 获取最后一条消息
```javascript
// 获取短期记忆（最近10条）
const history = await roche.memory.getShortTerm({
  conversationId: conv.id,
  limit: 10
});

// 取最后一条消息
if (history && history.length > 0) {
  const lastMsg = history[history.length - 1];
  lastMessage = lastMsg.text || lastMsg.content || '开始新对话...';
  lastTimestamp = lastMsg.timestamp || Date.now();
  
  // 截断过长的消息
  if (lastMessage.length > 50) {
    lastMessage = lastMessage.substring(0, 50) + '...';
  }
}
```

### 按时间排序
```javascript
// 按最后消息时间排序（最新的在前面）
conversationsWithInfo.sort((a, b) => b.lastTimestamp - a.lastTimestamp);
```

### 界面渲染
```javascript
<div style="display: flex; justify-content: space-between;">
  <div>[联系人名字]</div>
  <div>[3分钟前]</div>
</div>
<div>[最后一条消息内容...]</div>
```

---

## 📊 用户体验对比

### 修复前
| 信息 | 显示内容 | 实用性 |
|------|---------|--------|
| 标题 | 新建私信 | 🟡 |
| 摘要 | 记忆摘要 | 🔴 不直观 |
| 时间 | 无 | 🔴 缺失 |
| 排序 | 无序 | 🔴 混乱 |

### 修复后
| 信息 | 显示内容 | 实用性 |
|------|---------|--------|
| 标题 | 选择联系人 | 🟢 清晰 |
| 消息 | 最后一条消息 | 🟢 直观 |
| 时间 | 3分钟前 | 🟢 实用 |
| 排序 | 按时间倒序 | 🟢 高效 |

---

## 🎯 使用说明

### 打开联系人列表
1. 进入"私信"页面
2. 点击右上角蓝色"写私信"按钮
3. 或点击右下角蓝色浮动按钮

### 查看联系人
- 每个联系人显示最后一条消息
- 显示消息时间（如"刚刚"、"3分钟前"）
- 最近聊天的在最上面
- 点击联系人即可打开聊天

### 搜索联系人
- 在搜索框输入名字
- 实时过滤联系人列表

---

## 📦 部署信息

**版本号**：v5.6.3  
**类型**：功能优化

**安装链接**：
```
https://raw.githubusercontent.com/zxinyi404-maker/-roche-twitter/main/manifest.json?v=20260731990000
```

**代码变更**：
- `showNewMessageDialog()` - 优化联系人列表显示
- 从显示"记忆摘要"改为"最后一条消息"
- 新增时间显示和排序逻辑

---

## 🎉 总结

v5.6.3 修复了联系人列表的显示问题！

**核心改进**：
1. ✅ 显示最后一条消息（更直观）
2. ✅ 显示消息时间（更实用）
3. ✅ 按时间排序（更高效）
4. ✅ 标题更改为"选择联系人"（更清晰）

**用户反馈驱动**：
感谢用户提出："这个记录要和 Twitter 聊天内容同步哈"

**现在联系人列表和私信列表完全同步了！** 🎊

---

*发布日期：2026-07-31*  
*开发者：zxinyi404*  
*GitHub：https://github.com/zxinyi404-maker/-roche-twitter*
