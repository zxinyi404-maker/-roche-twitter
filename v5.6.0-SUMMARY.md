# Twitter 插件 v5.6.0 - NPC 智能回复系统

## ✅ 本次更新核心功能

### NPC 智能回复系统
**问题**：v4.3.0 下一步计划中最重要的功能 - NPC 需要能够智能回复用户的推文

**实现**：
- ✅ NPC 基于兴趣度智能判断是否回复
- ✅ 回复内容完全符合 NPC 人设和说话风格
- ✅ 自然延迟触发（2-5秒），避免刷屏
- ✅ 完整的回复限制机制
- ✅ 实时通知推送

**效果**：
- 兴趣度越高，NPC 回复你的概率越大
- 每个 NPC 每天最多回复 5 次
- 每条推文最多 2 个 NPC 回复
- 只回复真实用户，避免无限循环

---

## 🎯 智能回复机制

### 回复触发条件
1. **兴趣度阈值**: NPC 对用户的兴趣度 ≥ 0.4
2. **基础概率**: 30%（兴趣度越高，概率越大）
3. **今日限额**: 每个 NPC 每天最多回复 5 次
4. **推文限制**: 每条推文最多 2 个 NPC 回复

### 回复概率计算
```javascript
const replyChance = 0.3 * (interest / 0.4);

// 示例：
// 兴趣度 0.4 → 回复概率 30%
// 兴趣度 0.6 → 回复概率 45%
// 兴趣度 0.8 → 回复概率 60%
// 兴趣度 1.0 → 回复概率 75%
```

### 如何提高兴趣度
- 点赞 NPC 的推文: +0.05
- 转发 NPC 的推文: +0.1
- 回复 NPC 的推文: +0.15
- 关注 NPC: +0.3

---

## 🔧 技术实现

### 新增函数（4个核心函数）

**1. npcSmartReply() - 智能回复判断**
```javascript
async function npcSmartReply(tweetId, roche) {
  // 1. 只回复真实用户的推文（不回复 NPC）
  // 2. 不回复回复（避免无限循环）
  // 3. 获取对该用户有兴趣的 NPC
  // 4. 检查兴趣度阈值和今日回复次数
  // 5. 计算回复概率，随机选择 1-2 个 NPC
  // 6. 触发回复生成
}
```

**2. generateNPCReply() - 回复内容生成**
```javascript
async function generateNPCReply(npcId, originalTweet, roche) {
  // 1. 优先使用自定义后端 API
  // 2. 降级使用通用 API 配置
  // 3. 构建符合 NPC 人设的提示词
  // 4. 生成回复内容（最多 280 字）
  // 5. 创建回复推文
  // 6. 发送通知
}
```

**3. sendNPCReplyNotification() - 回复通知**
```javascript
async function sendNPCReplyNotification(npcId, originalTweet, reply, roche) {
  // 1. 使用 roche.notification.send API
  // 2. 显示 NPC 头像、名字、回复内容
  // 3. 支持"查看"和"忽略"操作
  // 4. 添加到插件内通知列表
}
```

**4. startNPCReplySystem() - 定期检查系统**
```javascript
function startNPCReplySystem(roche) {
  // 每 3 分钟检查一次
  setInterval(async () => {
    // 1. 获取最近发布的用户推文
    // 2. 过滤掉 NPC 推文和回复
    // 3. 为每条推文尝试触发 NPC 回复
  }, 3 * 60 * 1000);
}
```

### 新增数据结构

```javascript
// 1. NPC 回复配置
const NPC_CONFIG = {
  enableAutoReply: true,
  replyProbability: 0.3,
  maxRepliesPerNPCDaily: 5,
  minInterestForReply: 0.4,
  replyCheckInterval: 3
};

// 2. 推文数据结构扩展
tweet = {
  // ... 原有字段
  replyTo: number  // 回复的推文 ID（新增）
}

// 3. NPC 回复计数
twitterData.npcReplyCounts = {
  [npcId]: { date: '2026-07-31', count: 3 }
}
```

### 触发机制

**用户发推文时触发**
```javascript
async function postTweet(roche, content) {
  // ... 创建推文
  
  // 2-5秒后随机触发 NPC 回复
  setTimeout(() => {
    npcSmartReply(tweet.id, roche);
  }, 2000 + Math.random() * 3000);
}
```

**定期检查系统触发**
```javascript
// 每 3 分钟检查一次最近的推文
setInterval(async () => {
  // 获取最近的用户推文
  // 为每条推文尝试触发 NPC 回复
}, 3 * 60 * 1000);
```

---

## 📦 部署信息

**版本号**：v5.6.0  
**代码变更**：11331 → 11690 行（+360 行）

**安装链接**：
```
https://raw.githubusercontent.com/zxinyi404-maker/-roche-twitter/main/manifest.json?v=20260731900000
```

**更新方式**：
1. 完全卸载旧版本
2. 清除浏览器缓存
3. 重新安装新版本

**Git 提交**：
```bash
commit 5a3e8fd
feat: v5.6.0 - NPC 智能回复系统
```

---

## 🎯 使用说明

### 启用智能回复

1. 打开 Twitter 插件
2. 进入"设置" → "NPC 系统设置"
3. 确保"启用 NPC 系统"已开启 ✅
4. 确保"启用智能回复"已开启 ✅

### 获得 NPC 回复

1. **提高兴趣度**：与 NPC 互动（点赞、转发、回复、关注）
2. **发布推文**：发一条普通推文
3. **等待回复**：2-5 秒后，感兴趣的 NPC 可能会回复
4. **查看回复**：系统通知实时弹出

### 回复规则

- ✅ 只回复真实用户的推文
- ✅ 兴趣度越高，回复越频繁
- ✅ 每个 NPC 每天最多回复 5 次
- ✅ 每条推文最多 2 个 NPC 回复
- ✅ 回复延迟 2-5 秒，模拟真实互动

---

## 📊 代码统计

**新增文件**：
- `UPDATE_v5.6.0.md` - 完整更新日志

**修改文件**：
- `manifest.json` - 版本号、描述、entry URL
- `plugin.js` - 新增智能回复系统（+360 行）

**核心改动**：
- NPC_CONFIG：新增 5 个回复配置项
- twitterData：新增 2 个数据字段
- 新增 4 个核心函数（~300 行）
- 修改 postTweet() 触发回复
- 修改 initNPCSystem() 启动回复系统
- 设置页面：新增智能回复开关

---

## 🎉 总结

v5.6.0 成功实现了 **NPC 智能回复系统**！

**核心价值**：
1. ✅ Twitter 插件更加生动和互动
2. ✅ NPC 不再只是发帖，还会主动回复你
3. ✅ 基于兴趣度的智能推荐，越互动越了解你
4. ✅ 完整的限制机制，避免刷屏

**下一步计划**：
- ⏳ 可自定义 NPC 发帖频率和内容风格
- ⏳ 支持导入/导出 NPC 人设
- ⏳ NPC 智能推荐系统优化
- ⏳ 支持 NPC 之间的互动对话

---

*发布日期：2026-07-31*  
*开发者：zxinyi404*  
*GitHub：https://github.com/zxinyi404-maker/-roche-twitter*
